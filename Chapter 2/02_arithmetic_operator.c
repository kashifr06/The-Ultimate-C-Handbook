#include <stdio.h>

int main()
{
    int a = 4;
    int b = 3;
    int c = a + b;
    printf("The value of a is %d and b is %d and sum is %d\n", a, b, c);
    printf("The remainder when a is divided by b is %d\n", a % b);

    return 0;
}