#include <stdio.h>

int main()
{
    // condition? expression-if-true : expression-if-false
    int a = 235, b = 99;
    a > b ? printf("a is the larger than b\n") : printf("b is the larger than a\n");

    return 0;
}